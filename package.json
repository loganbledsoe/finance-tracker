 {
  "name": "finance-tracker-monorepo",
  "private": true,
  "engines": { "node": "20.x" },

  "scripts": {
    "heroku-prebuild": "npm --prefix client install --include=dev && npm --prefix server install --include=dev",
    "heroku-postbuild": "npm --prefix client run build && npm --prefix server run build",
    "dev": "concurrently \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "start": "node server/dist/index.js"
  },

  "devDependencies": {
    "concurrently": "^8"
  }
}
